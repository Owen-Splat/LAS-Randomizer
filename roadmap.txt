- disable UI stuff that isn't made yet to avoid confusion
    don't forget to add a setting for random environments

- starting hearts

- instrument shuffle options

- include external exluanch output download, add the ini config file to the rando so settings can be edited

- play 2 or 3 seeds start to finish with a couple different settings combinations

- release dev build, pray that I didn't overlook anything obvious

----------
AFTER PUBLIC DEV BUILD

- Try to use ItemGoldenLeaf and ItemSmallKey interchangeably to control compass alarm
    Probably need some small code changes to enemies that drop golden leaves so they don't randomly spawn

- trendy (TBD)
    trendy just needs to be able for us to change string table stuff in the .bfres file
    there looks to be a pattern in the string table, might be able to just edit some strings to short replacements and pad
    randomized physics will be fun too, maybe for a later update though since it'll take time making sure nothing becomes impossible

- check for any use of FlowControl::CompareFloat
    if unused, it's a free event for us to rewrite into anything we want to do
    currently i'm thinking it can be used for companion rando
    if we do, make every telephone booth have bowwow and rooster to swap between

----------
GENERAL FUTURE STUFF TO LOOK INTO

- eventflow edits through code gets pretty messy and hard to follow
    create our own type of flowchart file and write a parser that'll translate them into the .bfevfl files
    this reduces the amount of code and will just be plain easier to edit

- create a Randomizer.bfevfl to handle giving all items, everything else will subflow to this
    tried this at one point, rapids and seashell mansion crash for some reason, will need to look into it more
    now that GettingFlags are set through code, our ItemGetManager is much smaller so this isn't as needed

- barren dungeons

- proper tricks toggle

- work on custom text
    use custom text to make tunics more intuitive

- individual tricks toggles

- make separete repo containing custom models to be used optionally with rando
    for songs, capacity upgrades, and even custom items
    we could also potentially use EoW models if the user has the RomFS

- fix dampe logic for instrument shuffle
    logic currently assumes that if you have the instrument, you have all the items needed to clear the rooms
    this is obviously not true with instrument shuffle, but logic is complex and I do not care enough for it


----------
After I'm happy with the state of everything, it'll be time to work on AP support :)
